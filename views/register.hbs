<section>
    <div class="signin">
        <div class="signin__content" tabindex="0">
            <h2>Signup</h2>
            <p>We do not share your personal details with anyone.</p>
        </div>
        <div class="signin__form">
            <form>
                 {{#each registerData}}
                <div class="signin__form__input">
                    <input type="{{type}}" name="{{label}}" required>
                    <label>{{label}}</label>
                </div>
                {{/each}}
                <div class="signin__form__input">
                    <button>Signup</button>
                </div>
            </form>
        </div>
    </div>
</section>